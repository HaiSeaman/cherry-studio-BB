# 06 · 数据存储与状态管理

## 1. Redux Toolkit + redux-persist（渲染层）

### Store 组织（`src/renderer/src/store/index.ts`）

- `configureStore` 组合约 18 个 reducer；持久化配置：`persistReducer({ key:'cherry-studio', version:4, migrate, blacklist:[runtime, messages, messageBlocks, tabs, toolPermissions, paint] })`。
- 历史迁移函数已清理：`migrate.ts` 只保留覆盖 v0~v4 净效果的纯函数（曾多达 216 个）。
- rehydrate 完成后 dispatch `mergeDefaults()` 补齐新增快捷键，并 `invoke(IpcChannel.ReduxStoreReady)` 通知主进程。
- 跨窗口同步：`storeSyncService.createMiddleware()` + `setOptions({ syncList:['assistants/','settings/','llm/','selectionStore/'] })`，广播 action 带 `fromSync` 元数据防死循环。
- 导出类型化 hooks `useAppDispatch/useAppSelector/useAppStore`，并把 store 挂到 `window.store`。

### Slice 清单

| Slice | 说明 |
|---|---|
| `assistants` | 助手 |
| `llm` | 模型/服务商 |
| `settings` | 应用设置（最大，~33KB） |
| `runtime` | 运行时态（不持久化） |
| `newMessage` / `messageBlock` | **新消息模型**：消息与内容块分离存储（V2 重构核心） |
| `shortcuts` / `minapps` / `websearch` / `mcp` / `selectionStore` / `tabs` / `inputTools` / `translate` / `toolPermissions` / `backup` | 各功能域 |
| 页面级：`paint`、`musicSettings`、`hubSettings`、`iptvSettings` | 从 pages 目录引入；`iptvSettings`（v1.9.0）persist 默认持久化、不在 `storeSyncService.syncList`（纯本地不上传） |

### 快捷键 / 小程序状态（v1.10.3 变更）

**`shortcuts` slice**

- `voice_input` 默认快捷键**写死 `['Ctrl','\`']`**（原 `['Meta','Shift','\`']` 已删除）。改它必须同步三处：默认值 `packages/shared/config/constant.ts`、设置页提示文案、主进程注释。
- `mergeDefaultShortcuts()` 只修 `system`/`editable`，**不动 `shortcut` 值**。v1.10.3 加了唯一特例：把仍是旧默认值 `['Meta','Shift','\`']` 的 `voice_input` 升级为 `Ctrl+\``；用户自定义的组合保留（`CommandOrControl` 由主进程 `KEYCODE_MAP` 兜底映射）。
- 导出 `selectVoiceInputEnabled(state)` —— 语音输入开关的状态**就是 `voice_input.enabled`**，模型设置页与快捷键设置页共用这一份，避免 `'voice_input'` magic string 散布。
- 三个 reducer（`updateShortcut`/`toggleShortcut`/`resetShortcuts`）内部已自带 `window.api.shortcuts.update()` → IPC → 主进程重注册 → `voiceKeyboardManager.sync()`，所以「改快捷键/开关」**不需要新增 IPC**。

**`minapps` slice**

- 三个列表：`enabled`（显示区）/ `disabled`（隐藏区）/ `pinned`（置顶）。**v1.10.3 起不再按地区过滤**：`useMinapps()` 返回全量应用，隐藏只由用户在设置页拖拽决定。原先 `supportedRegions` 不含 `Global` 的 17 个国内应用（豆包/通义/讯飞/文心/智谱/百川/阶跃/海螺/ima/天工/纳米/小艺/WPS AI/知乎直答/商汤/百度AI搜索/Minimax）会在「地区=全球」时被静默隐藏，用户以为被删了。
- 连带删除的死字段：`settings.minAppRegion` + `setMinAppRegion`、`runtime.detectedRegion` + `setDetectedRegion`、类型 `MinAppRegionFilter`；主进程 `utils/ipService.ts`、IPC `App_GetIpCountry`、preload `api.getIpCountry` 一并删除。
- 保留项：`supportedRegions` 数据与 `MinAppRegion` 类型（纯标注，删 57 处不划算）。

> ⚠️ V2 重构期冻结改动标记位于 `store/index.ts` 与 `databases/index.ts` 文件头。

### 异步逻辑中枢

无 redux-saga/thunk 中间件堆叠，异步集中在 **`store/thunk/messageThunk.ts`（~1500 行）**：
`sendMessage:463`、`resendMessageThunk`、`regenerateAssistantResponseThunk`、`loadTopicMessagesThunk`、`initiateTranslationThunk`、`saveMessageAndBlocksToDB`、节流落库 `throttledBlockUpdate` 等 —— 是消息收发与持久化的唯一枢纽。

辅助方案：@tanstack/react-query 处理服务端型查询；页面内 Zustand 风格 store（如音乐 playerStore，经 widgetBridge 常驻挂件窗）。

## 2. Dexie / IndexedDB（渲染层本地库）

单文件库 **`CherryStudio`**（`src/renderer/src/databases/index.ts:28`，`chromeTransactionDurability:'strict'`），当前版本 **15**，升级脚本在 `databases/upgrades.ts`（upgradeToV5/V7/V8/V13/V14）：

| 表 | 用途 |
|---|---|
| `files` | 文件附件元数据（id/name/path/size/ext/count…） |
| `topics` | 聊天话题（id 主键，messages 数组；可选 type/name/updatedAt 区分绘画会话） |
| `settings` | K-V 杂项设置 |
| `quick_phrases` | 快捷短语 |
| `message_blocks` | 新消息模型的块存储（索引 messageId、file.id）—— 消息内容拆块 |
| `music_tracks`(++id, &filePath 唯一) / `music_folders`(&path) / `radio_favorites`(&url) | v11 音乐三表。`radio_favorites` 以 `url` 为主键存 FM 收藏台（连同板块数据里的 `badge`/`desc` 一起快照），桌面挂件直读该表渲染电台网格 |
| `hub_notes` / `hub_todos`(++id,status) / `hub_alarms`(++id) / `hub_day_notes`(++id,date) / `hub_activity`(&date) / `hub_note_history`(++id,noteId) | v12 闹钟便签六表 |
| `habits`(id, order, archived) / `habit_records`([habitId+date], date) | v13 打卡两表（习惯定义 + 每日打卡记录，复合主键保证幂等；`date` 索引供月历按区间查询） |
| `kb_bases` / `kb_files` / `kb_chunks` / `kb_search_index` | v14 知识库四表（库/文件/切块向量/检索索引），进 `stores()` 纯声明无存量迁移 |
| `iptv_playlists`(++id, &url) / `iptv_channels`(++id, playlistId) / `iptv_favorites`(url, addedAt) / `iptv_history`(url, playedAt) | **v15 IPTV 四表**（v1.9.0）：播放列表源 / 频道缓存 / 收藏快照 / 最近观看快照。**收藏与历史以 `url` 为主键**——与播放列表生命周期解耦（更新=清空重建频道表，自增 channelId 全变亦不影响收藏），url 天然去重（重复收藏 = put/delete，重复观看 = put 更新 playedAt）；`iptv_playlists` 的 `&url` 唯一索引防重复添加同一源 |

> IPTV 数据写入约定：M3U 文本统一走主进程 `fs.readText`（自动 chardet 编码检测，含 GBK/BOM）；解析用 `iptv-playlist-parser` 的 `parse`（缺失属性返回 `''`，入库前归一化为 null）；收藏/最近观看快照表的反查对象由 `IptvPage.toChannel` 构造（无需 playlistId）。

> FM 电台数据约定（v1.10.2）：四个板块的电台清单是**打包内置静态数据**（`services/stations.json` → `radioBoards.ts`），不走网络、不设缓存表，也没有"刷新有效期"概念；用户自建的电台存 Redux `musicSettings.customStations`（上限 500 条、按 url 去重），由 `getBoardStations()` 并入各板块列表**尾部**（与板块台撞 url 时**自定义优先**，保证 ✕ 能真正删掉它）；被 ✕ 隐藏的板块台 url 存 localStorage `music_radio_excluded`（`radioCache.ts`），语义是"隐藏"而非删除。播放列表喂给全局单例 `playerStore`（`setStations`），换台即在该列表内循环。

> 打卡统计口径（当前连续/最长连续/完成率/EMA 强度指数/星期分布/自然年统计）全部收敛于 `pages/habits/services/stats.ts` 纯函数模块，口径规则见 `docs/superpowers/specs/2026-08-28-habit-tracker-design.md#44-统计口径实现规则含-skip-处理`；**跳过(skip)日不算应打卡日、不断卡**，三处口径保持一致。
>
> 打卡 JSON 导入是**替换式写库（不可逆）**，入口必须经过 `habitService.parseHabitsBackup` 行级校验（schema / ISO 补零日期 / 状态枚举 / 引用完整性），损坏文件在写入前抛错拒绝；v1.7.4 起该函数为唯一校验出口。月历「漏卡」判定只作用于创建日（`createdAt`）之后且今天之前的日子，创建前格子锁定不可打卡。

已废弃表：`knowledge_notes`(v10 移除)、`translate_history` / `translate_languages`(v10 移除)。

主进程侧无 SQLite 业务库；文件二进制存于应用数据目录（FileStorage 服务管理），元数据在 Dexie。

## 3. 主进程配置存储

- **electron-store**（ConfigManager 单例）：语言、主题、托盘、缩放、快捷键、划词助手等强类型配置。
- 语音输入配置（`voiceInput`，v1.10.0）：`provider`（qwen/doubao/tencent）+ 三家各自的密钥/模型字段；读取时做**字段级默认值合并**（持久化数据残缺时 `start()` 不会访问到 undefined 而崩溃）。
- 配置变更经 `setAndNotify` 同步广播给渲染层。

## 4. 备份与恢复（BackupManager）

三种目标端：**本地目录 / WebDAV / S3**。

```
备份：backup() → backupToLocalDir | backupToWebDAV | backupToS3 （进度上报）
恢复：restore() → restoreFromLocalBackup | restoreFromWebDAV | restoreFromS3
启动：handleStartupRestore():84 —— 最高优先级检查恢复标记，必要时先于窗口创建执行
重置：resetData():763
辅助：checkConnection / checkS3Connection / createDirectory / listLocalBackupFiles / delete*
```

设置页 DataSettings 提供对应 UI；云端凭据存于 electron-store。

> v1.10.1 加固：远端恢复（WebDAV/S3）的本地落盘路径经 `path.basename` 净化（防 `../` 越界写出备份目录）、恢复完成后清理下载副本（try/finally，与备份收尾对齐）、`WebDav.putFileContents` 未初始化改为抛错（此前被调用方当成成功结果）。
> **已知未修**：所有备份/恢复共用固定 `this.tempDir` 且各自收尾删除，并发执行会互相破坏（备份损坏/恢复失败）——需按「每操作唯一临时目录」单独排期，属数据关键路径。

## 5. 数据流小结

```
UI 事件 → useMessageOperations/hooks → messageThunk → ApiService → aiCore → AI SDK
                                    ↓
                          Dexie (topics/message_blocks/files)
                                    ↓ (节流落库 throttledBlockUpdate)
跨窗口：StoreSync middleware 广播白名单 slice
主进程：electron-store(ConfigManager) + 文件系统(FileStorage) + 云端(WebDAV/S3)
```

## 6. 状态陷阱：assistant 快照 vs Redux 实时对象（v1.7.3 收口）

### 问题

`assistants` slice 的 `addTopic` / `removeTopic` 经 Immer 会**换掉 store 里的 assistant 对象引用**。若组件把 assistant 存进 `useState`（或由父组件以快照形式下发），该副本的 `topics` 会**永远停在挂载那一刻**。

v1.7.3 之前 `HomePage` 正是 `useState(assistants[0])`，而 `PaintWorkspace` 又用 `assistant.topics` 做会话归属守卫，于是：

```
新建话题 → Redux 已含该话题，但快照里没有
        → 守卫判定「不属于本助手」→ validTopicId = null
        → 内容区空态，输入区每次生成都再建一个孤儿话题
        → 图片照常落盘，但用户在当前会话里永远看不到
```

这与「话题列表能看到、点进去却空白」并存——因为 `PaintHistoryList` 当时已自行回 Redux 取数，两者数据源不一致。

### 约定（强制）

1. **只存 id，不存对象**：`HomePage` 的 state 只保留 `activeAssistantId`，`activeAssistant` 用 `useMemo` 从 Redux 派生（兜底顺序：`find(id)` → 路由带来的 `state.assistant` → 模块级 `_activeAssistant` → `assistants[0]`）。
2. **凡读 `assistant.topics` 先过 `useLiveAssistant`**（`hooks/useAssistant.ts`）：
   - `resolveLiveAssistant(assistants, assistant)` — 纯函数，按 id 取最新助手，取不到回退入参（便于单测）
   - `useLiveAssistant(assistant)` — 上面的 hook 版，selector 返回 store 内对象，引用稳定、不会触发额外渲染
   - `resolveValidTopicId(assistant, activeTopic)` — 会话归属守卫，两个 Workspace 共用；不命中一律返回 `null`
3. **生成期间禁止切换/新建会话**（生图 + 视频四处入口）：在途生成的结果始终写回**发起时的** topic，一旦期间切到新会话，用户眼前就是空会话，表现与「生成失败」无异。按钮需 `disabled`，且 `new_topic` 等快捷键入口必须单独加 `isGenerating` 拦截（`useShortcut` 不受按钮 `disabled` 约束）。
4. **不要用 `getDefaultTopic()` 当「重置」哨兵**：它返回随机 id 的幽灵话题（不在 db、不在 Redux），会污染 `newMessage` slice。删除当前会话时应从**剩余 topics** 里挑下一个；没有剩余就不动 `activeTopic`，交给归属守卫判空态。
5. **reducer 必须是纯函数，不得就地改写 action.payload**（v1.10.1 收口）：payload 是调用方持有的对象，被 Immer 冻结后再写入会直接抛错（`updateTopic` 曾就地写 `newTopic.updatedAt`）；同时「`assistant.topics[]` 不携带消息」是既有不变量（消息存在 messages slice），改相关 reducer 时用**产出新对象**表达，不要对旧对象赋值。回归测试：`store/__tests__/assistantsUpdateTopic.test.ts`。
6. **`getTopicById` 找不到话题时返回 `undefined`**（v1.10.1 收口）：此前返回 `{...undefined, messages}` 这种缺 `id/name` 的残缺对象，下游按 `Topic` 使用会崩；调用方必须各自判空。

### 排查提示

遇到「数据已落盘/写入成功，但 UI 不显示」类问题，沿 `落盘回调 → Dexie 写入 → Redux → 组件守卫 → 渲染` 逐层定位，**优先怀疑同一份数据存在两个来源（快照 vs Redux）**。相关回归测试：`src/renderer/src/pages/paint/__tests__/workspaceTopicGuard.test.ts`（用真实 reducer 驱动纯函数守卫）。
