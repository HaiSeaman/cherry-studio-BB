# 03 · 主进程模块（src/main）

## 1. 入口文件

| 文件 | 职责 |
|---|---|
| `index.ts` | 应用入口（253 行）：启动编排、单实例锁、协议注册、生命周期清理 |
| `bootstrap.ts` | 副作用模块：打包环境 `initAppDataDir()`；命令行参数触发的 appData 目录迁移（Windows 下 `copyOccupiedDirsInMainProcess` 在渲染进程启动前复制被占用目录） |
| `config.ts` | dev 模式 userData 加 `Dev` 后缀、titleBarOverlay 颜色常量、`CHERRYAI_CLIENT_SECRET` |
| `ipc.ts` | **IPC 注册中心**（792 行）：唯一导出 `registerIpc(mainWindow, app)`，集中绑定全部通道 |
| `constant.ts` / `configs/` | 常量与静态配置 |

## 2. 服务层总览（src/main/services）

### 2.1 窗口与界面

| Service | 文件 | 职责 |
|---|---|---|
| **WindowService** | `WindowService.ts:165` | 所有窗口生命周期：主窗口（Mica/vibrancy/自定义标题栏）、迷你快速助手窗、音乐/便签挂件窗（内部 `WidgetWindowController` 统一封装）；含 window-open 与崩溃重启监控 |
| **ThemeService** | `ThemeService.ts:12` | 主题同步单例：`setTheme(theme):38` 写 nativeTheme + ConfigManager，更新时同步所有窗口标题栏 overlay 并广播 `ThemeUpdated` |
| **AppMenuService** | — | macOS 应用菜单 |
| **TrayService** | — | 系统托盘 |
| **ContextMenu** | — | 右键菜单 |

**WindowService 关键方法**：`createMainWindow():174`、`showMainWindow():512`、`toggleMainWindow():576`、`createMiniWindow()/showMiniWindow()/hideMiniWindow()/closeMiniWindow()/setPinMiniWindow()`、挂件系列 `showMusicWidget()/toggleMusicWidget()/closeMusicWidget()/setMusicWidgetPin/Lock/Size()`、`quoteToMainWindow(text):836`、`getMainWindow()` 等；私有 `setupWebContentsHandlers:363`。

### 2.2 配置与状态

| Service | 文件 | 职责 |
|---|---|---|
| **ConfigManager** | `ConfigManager.ts:49` | 基于 electron-store 的主进程配置单例。类型化 getter/setter 20+（language/theme/tray/zoomFactor/shortcuts/selectionAssistant 系列/disableHardwareAcceleration/useSystemTitleBar/hubExecEnabled…）+ 通用 `get(key)` / `set(key,value,isNotify)` / `setAndNotify(key,value)` |
| **ReduxService** ⚠️deprecated | `ReduxService.ts:44` | v2 移除。主进程反向读取/派发渲染层 Redux store（等待 `ReduxStoreReady` 后 executeJavaScript 调 `window.store`）。方法 `select<T>(selector):80` / `dispatch(action):96` |
| **StoreSyncService** ⚠️deprecated | `StoreSyncService.ts:31` | 多窗口 Redux action 同步总线：`subscribe/unsubscribe/syncToRenderer/registerIpcHandler`，广播打 `fromSync` 元数据防死循环 |

### 2.3 AI / MCP

| Service | 文件 | 职责 |
|---|---|---|
| **MCPService** | `MCPService.ts:163` | MCP 客户端中枢：为每个 server 创建/缓存 stdio/SSE/in-memory `Client`；工具/Prompt/资源调用；连通性检测；日志缓冲与清理 |

**McpService 关键方法**：`initClient(server):274`、`listTools/callTool/listPrompts/getPrompt/listResources/getResource`、`callToolById(toolId,…):224`、`restartServer/stopServer/removeServer/cleanup`、`checkMcpConnectivity():874`、`abortTool(callId):1178`、`getInstallInfo():1015`、`listAllActiveServerTools():193`。

子目录：
- `mcp/`：`ServerLogBuffer`、OAuth provider/callback/storage；
- `remotefile/`：OpenAI/Gemini/Mistral 远端 File API 抽象。

### 2.4 数据与备份

| Service | 文件 | 职责 |
|---|---|---|
| **BackupManager** | `BackupManager.ts:55` | 备份/恢复/重置：本地目录、WebDAV、S3 三种目标，含启动恢复标记处理与进度上报 |
| **FileStorage** (fileManager) | `FileStorage.ts` | 最大服务（~1700 行）：上传/读写/移动/重命名/base64 图片转换、目录列举（ripgrep 加速+模糊匹配）、文件 watcher |
| **S3Storage** / **WebDav** | — | 云端存储后端实现 |
| **CacheService** | — | 缓存管理 |
| **LoggerService** (`@logger`) | — | 日志（支持 `CSLOGGER_MAIN_LEVEL` 等环境变量分级），src 下禁用裸 console 的统一出口 |

**BackupManager 关键方法**：static `handleStartupRestore():84`、`backup/backupToLocalDir/backupToWebdav/backupToS3`、`restore/restoreFromLocalBackup/restoreFromWebdav/restoreFromS3`、`resetData():763`、`checkConnection/checkS3Connection/listLocalBackupFiles/delete*`。

**FileStorage 公开面**（ipc.ts:462-498 绑定 30+ 方法）：``、`getFilePathById():1733`。

### 2.5 系统集成

| Service | 职责 |
|---|---|
| **ShortcutService**（函数式） | 全局/应用内快捷键：`registerShortcuts(window):103`、`unregisterAllShortcuts():227`、内部 `getShortcutHandler/registerZoomShortcut`（缩放快捷键 + 窗口焦点感知） |
| **SelectionService**（单例，~1600 行） | 划词助手：系统级文本选择钩子、悬浮工具条定位/显隐、动作弹窗池（预创建+栈式复用）、剪贴板写入。关键方法：`start():316/stop():373/quit():400/toggleEnabled():415/processSelectTextByShortcut():782/processAction():1306/hideToolbar():602/writeToClipboard():1523`、static `registerIpcHandler()`、工厂 `initSelectionService():1638` |
| **ScreenshotService** | 截图（依赖 electron-screenshots，原生 .node 绑定 asarUnpack） |
| **PowerMonitorService** | 电源事件监听（配合关机保存 `App_SaveData`） |
| **AutomationService** | AI 自动化任务调度器（主进程独立 30s tick） |
| **ProxyManager** | session 级代理管理（`proxy/` 子目录为 node 侧代理，构建产物 out/proxy 需 asarUnpack） |
| **ProtocolClient** | `cherry://` 深链注册与分发；`urlschema/` 子目录做协议 URL 业务分发（provider 配置导入、MCP 安装） |
| **AppService** | 开机自启 |
| **NotificationService** | 系统通知 |
| **ExportService** | Word 导出 |
| **DxtService** | `.dxt` MCP 扩展包安装 |
| **ObsidianVaultService** | Obsidian 仓库集成 |
| **MusicService** | 音乐库扫描/播放支撑 |
| **VersionService** | 版本记录（升级检测） |
| **SearchService** | 本地搜索 |
| **WebviewService** | webview 行为：`initSessionUserAgent()`、`setOpenLinkExternal(webviewId,isExternal)`（动态 setWindowOpenHandler）、`printWebviewToPDF`、`saveWebviewAsHTML`、`initWebviewHotkeys()` |
| **ExternalAppsService** | 外部应用检测（配置在 shared/externalApp/） |

## 3. 内置 MCP Server（src/main/mcpServers)

工厂 `factory.ts:createInMemoryMCPServer(name, args, envs):19` 按 `BuiltinMCPServerNames`（定义于 `src/renderer/src/types/index.ts:636`，形如 `@cherry/memory`）分发，共 9 个标准 MCP `Server` 实现 —— 内置 server 免起子进程，由 MCPService 直接 in-memory 使用：

| Server | 文件 | 功能 |
|---|---|---|
| memory (`@cherry/memory`) | `memory.ts:328 MemoryServer` | 本地知识图谱记忆：create/delete_entities、relations、add_observations、read_graph、search_nodes、open_nodes |
| sequentialthinking | `sequentialthinking.ts:146` | 结构化逐步推理工具 |
| brave-search | `brave-search.ts` | Brave 网页搜索 `brave_web_search` / `brave_local_search`（需 BRAVE_API_KEY） |
| fetch | `fetch.ts` | URL → Markdown 网页抓取 |
| filesystem | `filesystem/server.ts + tools/` | 受限沙箱文件操作 read/write/edit/ls/glob/grep/delete（baseDir 由 args/envs 解析） |
| dify-knowledge | `dify-knowledge.ts` | Dify 知识库检索（需 DIFY_KEY） |
| didi-mcp | `didi-mcp.ts` | 滴滴出行：地图搜索/估价/订单/司机追踪（仅中国大陆） |
| browser | `browser/controller.ts CdpBrowserController` | CDP 浏览器控制：open/execute(JS)/screenshot/snapshot/tabs/reset；普通(持久)/隐私(临时)两种模式；设 `setWindowOpenHandler` |
| hub | `hub/` | 元服务器：聚合所有活动 MCP 工具，暴露 meta-tools `list`（搜索）/`inspect`(JSDoc stub)/`invoke`(调用)/`exec`（vm 沙箱多工具编排，worker.ts 含原型链逃逸防护）；`hub/toolname.ts` 做 camelCase 工具名映射去重 |

## 4. 其他

- `integration/cherryai`：签名客户端（JS 实现）。
- `utils/`：主进程内部工具函数。
- 测试：`services/__tests__/`，由 vitest project `main` 覆盖（node 环境，setup 为 `tests/main.setup.ts`）。
